<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Confirmation</title>
    <style>
        body { font-family: Arial, sans-serif; background: #f8f9fa; margin: 0; padding: 20px; }
        .container { width: 90%; max-width: 600px; margin: 20px auto; background: #fff; padding: 20px; border-radius: 8px; box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1); }
        .logo { text-align: center; margin-bottom: 20px; }
        .logo img { max-width: 150px; }
        h2 { text-align: center; color: #333; }
        p { text-align: center; color: #555; }
        .order-details { background: #f8f9fa; padding: 15px; border-radius: 5px; margin-bottom: 20px; }
        .order-items { margin-top: 15px; }
        .order-item { display: flex; align-items: center; margin-bottom: 10px; padding: 10px; border-radius: 5px; background: #fdfdfd; }
        .order-item img { width: 60px; height: 60px; border-radius: 5px; margin-right: 15px; }
        .item-details { flex: 1; }
        .btn { display: block; width: 80%; margin: 10px auto; text-align: center; background: #ff6600; color: #fff; padding: 12px; text-decoration: none; border-radius: 5px; font-size: 16px; font-weight: bold; }
        .btn:hover { background: #e65c00; }
        .footer { text-align: center; margin-top: 20px; font-size: 14px; color: #555; }
        .social-icons { text-align: center; margin-top: 10px; }
        .social-icons img { width: 30px; margin: 0 5px; }
    </style>
</head>
<body>
    <div class="container">
        <!-- Company Logo -->
        <div class="logo">
            <img src="https://yourcompany.com/logo.png" alt="YourStore Logo">
        </div>

        <!-- Order Confirmation Message -->
        <h2>Thank You for Your Order, <%= user.name %>! 🎉</h2>
        <p>Your order <strong>#<%= order.id %></strong> has been placed successfully.</p>

        <!-- Order Summary -->
        <div class="order-details">
            <p><strong>Total Amount:</strong> ₹<%= order.totalAmount %></p>
            <p><strong>Estimated Delivery:</strong> <%= order.estimatedDelivery %></p>
        </div>

        <!-- Order Items -->
        <div class="order-items">
            <% order.items.forEach(item => { %>
                <div class="order-item">
                    <img src="<%= item.image %>" alt="<%= item.name %>">
                    <div class="item-details">
                        <strong><%= item.name %></strong><br>
                        <span>₹<%= item.price %> (x<%= item.quantity %>)</span>
                    </div>
                </div>
            <% }); %>
        </div>

        <!-- Call-to-Action Buttons -->
        <p><a href="<%= order.trackingLink %>" class="btn">📦 Track Your Order</a></p>
        <p><a href="<%= order.invoiceLink %>" class="btn" style="background:#007bff;">📄 Download Invoice</a></p>

        <p>If you have any questions, contact <a href="mailto:support@yourstore.com">support@yourstore.com</a>.</p>

        <!-- Social Media Links -->
        <div class="social-icons">
            <a href="https://facebook.com/yourstore"><img src="https://cdn-icons-png.flaticon.com/512/1384/1384053.png" alt="Facebook"></a>
            <a href="https://twitter.com/yourstore"><img src="https://cdn-icons-png.flaticon.com/512/1384/1384065.png" alt="Twitter"></a>
            <a href="https://instagram.com/yourstore"><img src="https://cdn-icons-png.flaticon.com/512/1384/1384063.png" alt="Instagram"></a>
        </div>

        <!-- Footer -->
        <div class="footer">
            <p>© 2024 YourStore. All rights reserved.</p>
        </div>
    </div>
</body>
</html>
